
// we use the props also or the argument name like this({name,age})
const FunProps = ({name,age,sub}) => {
  return (
    <div>
    my name is {name} and my age is {age}i am learning{sub}
    </div>
  )
}

export default FunProps